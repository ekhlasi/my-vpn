# تغییرات نسخه v10 (شفاف‌سازی برچسب کشور آی‌پی سالم + افزودن آی‌پی دستی + فعال/غیرفعال‌کردن هر اسلات کانفیگ + بازگشت به پیش‌فرض)

## مشکل ۱: «آی‌پی سالمی که گفتی آلمان/ترکیه‌ست، عملاً اونجوری کار نمی‌کنه»

### ریشه‌ی واقعی مشکل

تابع `detectColo` در `src/services/clean-ips.ts` برای فهمیدن اینکه هر آی‌پی کاندیدا الان به کدام دیتاسنتر
کلادفلر می‌رسد، مستقیماً **از خودِ ورکر** آن آی‌پی را دایل می‌کند (`cf.resolveOverride`). چون این درخواست از یک
آی‌پی کلادفلر به یک آی‌پی دیگر کلادفلر است، احتمالاً از داخل بک‌بون خصوصی خود کلادفلر رد می‌شود — نه از مسیر
واقعی اینترنت عمومی که یک اپراتور ایرانی برای رسیدن به همان آی‌پی anycast طی می‌کند. یعنی برچسب «آلمان» که
دیده می‌شود، جواب صادقانه‌ای به «این درخواست خاص الان کجا رسید» است، ولی هیچ تضمینی نیست که ترافیک واقعی
کاربر ایرانی هم به همان‌جا برسد — چون مسیریابی anycast را BGP هر شبکه/اپراتور جدا تصمیم می‌گیرد.

### راه‌حلی که بررسی و رد شد (برای صداقت کامل با شما)

فکر اولیه این بود که از خودِ check-host.net (که واقعاً روی گره‌های داخل ایران اجرا می‌شود) بخواهیم آدرس
`/cdn-cgi/trace` را روی هر آی‌پی کاندیدا فچ کند و مقدار `colo=` را از متن پاسخ بخوانیم — این می‌شد راه‌حل
واقعی و دقیق. اما مستندات API عمومی check-host.net (`check-http`) را بررسی کردم: خروجی آن فقط
`[موفقیت, تأخیر, متن وضعیت HTTP, کد وضعیت HTTP]` است و **هرگز متن پاسخ (body) را برنمی‌گرداند** — پس هیچ
راهی برای خواندن `colo=` از یک تست واقعی داخل ایران، از طریق همین سرویس، وجود ندارد. به‌جای اینکه یک «رفع
مشکل» ساختگی تحویل بدهم که در عمل کار نمی‌کند، این محدودیت را همین‌جا و داخل کد مستند کردم تا در آینده هم
اشتباهاً دوباره «حل‌شده» فرض نشود.

### چیزی که واقعاً اضافه شد

1. **هشدار صریح در داشبورد و کد**: برچسب کشور/دیتاسنتر حالا به‌صراحت به‌عنوان «راهنمای احتمالاتی، نه تضمین»
   معرفی می‌شود. ستون «وضعیت» (که واقعاً از ۵ گره داخل ایران تست می‌شود) قابل‌اعتمادتر از برچسب کشور است.
2. **افزودن آی‌پی/رنج پیشنهادی توسط ادمین** (تب «پنل‌ها»): چون لیست ثابت و کوچک `CANDIDATE_IPS` هرگز نمی‌تواند
   مثل کانال‌های وی‌پی‌ ایران لحظه‌ای به‌روز بماند، حالا می‌توانید همان لیست‌های «آی‌پی سالم آلمان/ترکیه» که جای
   دیگر می‌بینید را اینجا بچسبانید (هر خط یک IPv4 یا CIDR، با برچسب اختیاری) تا **همین سیستم** آن‌ها را مستقل
   و از گره‌های واقعی داخل ایران دوباره تست کند، پیش از اینکه چیزی تأیید/به کاربران داده شود.
   فایل‌های تغییریافته: `src/services/clean-ips.ts` (`parseCandidateInput`)، `src/db/queries.ts`
   (`custom_candidate_ips`)، `src/db/schema.sql`، `src/db/migration-v10.sql` (جدید)، `src/api/admin.ts`،
   `src/pages/admin.ts`.

## مشکل ۲: تنظیمات حرفه‌ای برای فعال/غیرفعال‌کردن هر کانفیگ + بازگشت به پیش‌فرض

دقیقاً مثل پنل ادمین شرکت‌های وی‌پی‌ان تجاری:

1. هر کدام از ۵ اسلات کانفیگ کاربر VIP (۲ تای آی‌پی سالم خودکار + ۳ تای آی‌پی تأییدشده) و همچنین کانفیگ
   تست/ترایال، حالا جدا از بقیه قابل خاموش/روشن‌کردن است. اسلات خاموش‌شده اصلاً در لیست اشتراک کاربر ظاهر
   نمی‌شود (کانفیگ تست به‌جای حذف‌شدن کامل، فقط بدون آی‌پی سالم و به دامنه‌ی خود ورکر برمی‌گردد — چون کاربر
   تست فقط همین یک کانفیگ را دارد و صفرکردنش شبیه اکانت خراب/منقضی به نظر می‌رسد).
2. دکمه‌ی «🔄 بازگشت به تنظیمات پیش‌فرض»: نام‌گذاری کانفیگ‌ها، روشن/خاموش‌بودن آی‌پی سالم، تنظیمات پنل‌ها
   (batch/rest) و هر ۶ اسلات بالا را به مقدار پیش‌فرض کارخانه برمی‌گرداند — بدون هیچ تأثیری روی کاربران، رمز
   ادمین، توکن تلگرام یا لیست آی‌پی‌های دستی‌ای که خودتان اضافه کرده‌اید.

فایل‌های تغییریافته: `src/db/queries.ts` (`resetVpnConfigSettings`, `getConfigSlotSettings`,
`VPN_CONFIG_RESETTABLE_KEYS`)، `src/services/subscription.ts` (`slotEnabled` در `buildUserSubscription`)،
`src/core/handler.ts`، `src/api/admin.ts` (`/config-slots`, `/config-slots/reset`)، `src/pages/admin.ts`.

## به‌روزرسانی دیتابیس موجود

اگر پروژه از قبل دیپلوی شده، این را یک‌بار اجرا کنید:

```
npx wrangler d1 execute <db-name> --remote --file=./src/db/migration-v10.sql
```

دیتابیس‌های تازه (فایل `src/db/schema.sql`) از اول این تنظیمات را دارند و نیازی به این مرحله ندارند.
